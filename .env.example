# Chave da API Anthropic (obrigatório)
ANTHROPIC_API_KEY=sk-ant-...

# Chave da API do Omni — gerada automaticamente no primeiro boot
# Depois de subir o Omni pela primeira vez, copie a chave exibida no log e coloque aqui
OMNI_API_KEY=
